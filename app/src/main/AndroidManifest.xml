<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.indaco.daggertestapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DaggerTestApp">
        <receiver
            android:name=".ui.screens.widget.MyWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/my_widget_info" />
        </receiver>

        <activity android:name=".ui.screens.main.bart.BartActivity" />
        <activity android:name=".ui.screens.onboarding.signup.SignUpParentActivity" />
        <activity
            android:name=".ui.screens.onboarding.launcher.LauncherActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.screens.onboarding.login.LoginActivity" />
        <activity android:name=".ui.screens.onboarding.welcome.WelcomeActivity" />
        <activity android:name=".ui.screens.onboarding.landing.LandingActivity" />
    </application>

</manifest>